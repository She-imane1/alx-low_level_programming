#include <stdio.h>
/**
 *main - point
 *@agrc: number of arguments
 *@argv: arguments
 *Return: number of arguments
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (argc - 1);
}
