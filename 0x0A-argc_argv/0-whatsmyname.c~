#include <stdio.h>
/**
 *main - point
 *@argv: the name of the program
 *@argc: the size of argv
 *Retur: always 0
 */

int main(int argc _attribute_((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
