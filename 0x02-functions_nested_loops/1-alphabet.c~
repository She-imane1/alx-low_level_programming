#include "main.h"
/**
 *main - point
 *print_alphabet - function writes the alphabet on a line.
 *Return: 0 (sucees)
 *
 */
void print_alphabet(void)
{
char c;

for (c = 'a' ; c <= 'z' ; c++)
{
_putchar(c);
}
_putchar('\n');
return (0);
}
