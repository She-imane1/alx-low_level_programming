#include <stdio.h>
/**
 *main - prints the alphabet in lowercase
 *then in uppercase followed by a new line
 *
 *Return: 0 (success)
 */
int main(void)
{
char m;
for (m = "a" ; m <= 'z' ; m++)
{
putchar (m);
}
for (m = 'A' ; m <= 'Z' ; m++)
{
putchar (M);
}
return (0);
}
